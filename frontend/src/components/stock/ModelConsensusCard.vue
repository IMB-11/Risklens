<template><article class="card"><div class="card-header"><h3 class="card-title">多模型共识</h3><span class="badge">{{ Math.round(models.confidence * 100) }}%</span></div><p class="muted">总体共识：{{ models.consensus }}</p><div class="list"><div v-for="item in models.items" :key="item.name" class="list-row row-between"><span>{{ item.name }} · {{ item.signal }}</span><strong>{{ Math.round(item.confidence * 100) }}%</strong></div><div v-if="!models.items?.length" class="list-row muted">真实接口暂未返回多模型明细，仅展示总体预测信号。</div></div></article></template>
<script setup>defineProps({ models: { type: Object, required: true } })</script>
